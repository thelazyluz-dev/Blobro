{
  "name": "blorbo-leaderboard-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker + D1 global leaderboard for Blorbo",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute blorbo-leaderboard --remote --file=./schema.sql",
    "test:integration": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.4",
    "@cloudflare/workers-types": "^4.20241106.0",
    "vitest": "^2.1.9",
    "wrangler": "^3.86.0"
  }
}
